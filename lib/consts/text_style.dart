import 'package:media_player/consts/consts.dart';

customTextStyle(
    {double? size = 14.0,
    Color? color = primaryColor,
    FontWeight? weight = smallWeight}) {
  return TextStyle(
      fontSize: size, fontFamily: poppins, fontWeight: weight, color: color);
}
